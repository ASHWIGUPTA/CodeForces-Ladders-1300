package a;
class test{
	/*
	 * public static String longestPalindrome(String s) { int n = s.length();
	 * boolean[][] p = new boolean[n][n]; int maxlenth = 0; int beginIndex = 0;
	 * 
	 * for (int i = n; i >= 0 ; i--) { //i must form n to 0 ,beacause update from
	 * outer index to inner for (int j = i; j < n ; j++) { if (i == j ){ p[i][j] =
	 * true; //base cases }else if (j == i + 1 || j == i + 2) { p[i][j] =
	 * s.charAt(i) == s.charAt(j);
	 * 
	 * }else { p[i][j] = s.charAt(i) == s.charAt(j) && p[i+1][j-1]; } prints(p,
	 * i,j); if (p[i][j] && j - i + 1 >= maxlenth){ // must be >= , if not ,would
	 * not update to the first index(if the first case as long as maxlength)
	 * maxlenth = j - i + 1; beginIndex = i; } } } return
	 * s.substring(beginIndex,beginIndex + maxlenth); }
	 * 
	 * private static void prints(boolean[][] p,int n, int q) {
	 * 
	 * // TODO Auto-generated method stub System.out.println("call "+n +" "+q );
	 * for(int i=0;i<p[0].length;i++) { for(int j=0;j<p.length;j++){
	 * System.out.print(p[i][j]+" "); } System.out.println(); } }
	 */

public static void main(String[] args) {
//System.out.println(test.longestPalindrome("aabaa"));	


}
}

